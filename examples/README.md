# AI CAPTCHA examples

- `solve_client.py` — a Python client that plays a full challenge via the JSON API.
- `embed_example.py` — shows how to embed AI CAPTCHA into an existing Flask app
  and protect routes with the decorators.
